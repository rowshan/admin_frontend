

<div class="panel-body">
  <div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
      <thead>
      <tr>
        <th>id #</th>
        <th>Name #</th>
        <th>slug</th>
        <th>province_id</th>

        <th></th>
        <th></th>
        <th></th>
      </tr>
      </thead>

      <!--<%#= @orders.each do|order| %>-->

      <tr>
        <!--<td><%#=link_to(order.invoice_id , order)%></td>-->
        <% @cities.each do |key, value| %>
            <td><%=  "#{value}" %></td>

        <% end %>
        <td><%= link_to "Show", city_management_path(@cities[:id]) %></td>
        <td><%= link_to "Edit",  edit_city_management_path(@cities[:id]) %></td>
        <td><%= link_to "Remove", city_management_path(@cities[:id]), method: :delete, data: { confirm: "Are you sure?" } %></td>


      </tr>

    </table>
  </div>
</div>
