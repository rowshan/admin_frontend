

<div class="panel-body">
  <div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
      <thead>
      <tr>
        <th>id #</th>
        <th>Code #</th>
        <th>City ID</th>

        <th></th>
        <th></th>
        <th></th>
      </tr>
      </thead>

      <!--<%#= @orders.each do|order| %>-->

      <tr>
        <!--<td><%#=link_to(order.invoice_id , order)%></td>-->
        <% @postals.each do |key, value| %>
            <td><%=  "#{value}" %></td>

        <% end %>
        <td><%= link_to "Show", postal_management_path(@postals[:id]) %></td>
        <td><%= link_to "Edit",  edit_postal_management_path(@postals[:id]) %></td>
        <td><%= link_to "Remove", postal_management_path(@postals[:id]), method: :delete, data: { confirm: "Are you sure?" } %></td>


      </tr>

    </table>
  </div>
</div>
