

<div class="panel-body">
  <div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
      <thead>
      <tr>
        <th>Plant Id but here for testing purpose  I used current_user_id#</th>
        <th>Company Name</th>
        <th>email</th>
        <th>Phone No.</th>
        <th>Address</th>
        <th>Tenant_id</th>
        <th></th>
      </tr>
      </thead>

      <tbody>
      <!--<%#= @orders.each do|order| %>-->

      <tr>
        <!--<td><%#=link_to(order.invoice_id , order)%></td>-->



        <% @plants.each do |key, value| %>
            <td><%=  "#{value}" %></td>

        <% end %>
        <td><%= link_to "Show", plant_management_path(@plants[:id]) %></td>
        <td><%= link_to "Edit",  edit_plant_management_path(@plants[:id]) %></td>
        <td><%= link_to "Remove", plant_management_path(@plants[:id]), method: :delete, data: { confirm: "Are you sure?" } %></td>

      </tr>





      </tbody>
    </table>
  </div>
</div>
