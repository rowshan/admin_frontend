

<div class="panel-body">
  <div class="table-responsive">
    <table class="table table-bordered table-hover table-striped">
      <thead>
      <tr>
        <th>id #</th>
        <th>Name #</th>
        <th>Code</th>

        <th></th>
        <th></th>
        <th></th>
      </tr>
      </thead>

      <!--<%#= @orders.each do|order| %>-->

      <tr>
        <!--<td><%#=link_to(order.invoice_id , order)%></td>-->
        <% @countries.each do |key, value| %>
            <td><%=  "#{value}" %></td>

        <% end %>
        <td><%= link_to "Show", country_management_path(@countries[:id]) %></td>
        <td><%= link_to "Edit",  edit_country_management_path(@countries[:id]) %></td>
        <td><%= link_to "Remove", country_management_path(@countries[:id]), method: :delete, data: { confirm: "Are you sure?" } %></td>


      </tr>

    </table>
  </div>
</div>
